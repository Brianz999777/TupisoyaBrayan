CREATE EXTENSION IF NOT EXISTS pg_trgm;

CREATE INDEX IF NOT EXISTS idx_propiedad_trgm ON propiedad 
USING gin ((
    coalesce(poblacion_prop, '') || ' ' || 
    coalesce(provincia_prop, '') || ' ' || 
    coalesce(cp_prop, '') || ' ' || 
    coalesce(direccion_prop, '')
) gin_trgm_ops);